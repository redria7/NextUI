{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make desktop",
            "type": "shell",
            "command": "make clean PLATFORM=desktop && make setup common PLATFORM=desktop",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        }
    ]
}